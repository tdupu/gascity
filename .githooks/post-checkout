#!/usr/bin/env bash
# The repo has no post-checkout gate of its own; this exists so .githooks can
# own core.hooksPath without dropping the beads post-checkout hook.
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
exec "$repo_root/.githooks/lib/beads-chain.sh" post-checkout "$@"
